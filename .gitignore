# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Scratch verification/capture harnesses, and the frames they write. Both are
# produced ad hoc while checking a build — by me and by reviewers driving it —
# and are deliberately not part of the source. The image rule exists because the
# scripts were ignored but their output was not, so every review pass left
# untracked PNGs in the repository root.
_*.mjs
_*.png
_*.jpg
